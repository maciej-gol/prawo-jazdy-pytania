[project]
name = "prawo-jazdy-setup"
version = "0.1.0"
description = "Data preparation scripts for the Prawo Jazdy practice app"
requires-python = ">=3.8"
dependencies = [
    "openpyxl>=3.1",
    "requests>=2.31",
]

[tool.uv]
dev-dependencies = []
